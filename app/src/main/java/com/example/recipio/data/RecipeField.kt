package com.example.recipio.data

import androidx.annotation.StringRes
import com.example.recipio.R

enum class RecipeField(val title: String) {
    Name(title = "name"),
    Category(title = "category")
}