package com.example.recipio.ui.theme

import androidx.compose.ui.graphics.Color

// Couleur orange pour les boutons (même pour les deux thèmes)
val ButtonColor = Color(0xFFE58E30)
val OnButtonColor = Color.White

// Couleurs claires
val LightBackground = Color.White
val LightSurface = Color(0xFFF9F9F9)
val LightSurfaceVariant = Color(0xFFF1F1F1)
val LightOnBackground = Color.Black
val LightOnSurface = Color.Black

// Couleurs sombres
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val DarkSurfaceVariant = Color(0xFF2A2A2A)
val DarkOnPrimary = Color.White
val DarkOnBackground = Color.White
val DarkOnSurface = Color.White